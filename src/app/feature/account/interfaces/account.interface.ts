export interface Account {
  id: string;
  accountTypeId: string;
  currencyId: string;
  institutionId: string;
  name: string;
  accountNumber: string;
  color: string;
  notes: string;
  balance: number;
}
